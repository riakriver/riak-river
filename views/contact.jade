extends layout
block content
  p Questions, comments? Drop us an
    a(href="http://www.appygram.com/")  appygram below.
  form(role="form", method="POST")
    div.form-group
      label(for="name") Name:
      input.form-control(type="text", id="name", name="name", placeholder="Enter name")
    div.form-group
      label(for="email") Email:
      input.form-control(type="email", id="email", name="email", placeholder="Enter Email")
    div.form-group
      label(for="message") Message:
      textarea.form-control(id="message", name="message", placeholder="Enter Message")
    div.checkbox
      label(for="cc") Send me a copy of message
      input(type="checkbox", id="cc", name="cc")
    button.btn.btn-default(type="submit") Submit

